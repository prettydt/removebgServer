# API Configuration
# Comma-separated list of allowed API keys
# Leave empty or comment out for development mode (no authentication)
ALLOWED_KEYS=your-api-key-1,your-api-key-2,your-api-key-3

# Model Configuration
MODEL_PATH=models/u2netp.onnx

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Note: Copy this file to .env and update with your values
# The .env file is gitignored and will not be committed
